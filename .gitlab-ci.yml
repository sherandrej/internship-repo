build-job:
 stage: build
 script:
   - echo "Hello, $GITLAB_USER_LOGIN!"
deploying:
 stage: deploy
 script:
   - sudo git pull origin
   #- git checkouut dev
   - cd "internship-repo/Docker"
  # - ls
   - sudo docker-compose -f "internship-repo/Docker/docker-compose.yml" up -d
   - echo "Deployed"
#   - docker-compose up -d
